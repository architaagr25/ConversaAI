# Python 3.12. FFmpeg must be available on the system path for audio conversion.

# --- Configuration and shared plumbing ---
python-dotenv>=1.0.1          # loads API keys from .env instead of hardcoding them
pydantic>=2.7                 # validates config and structured model output
pydantic-settings>=2.3        # maps environment variables onto typed settings objects
tenacity>=8.3                 # retry with backoff, so a rate-limited call is not a failed call
rich>=13.7                    # readable console output for the live nudge feed

# --- Language models ---
google-genai>=1.0.0           # Gemini, the reasoning model behind every conversation
groq>=0.11.0                  # speech-to-text, and a fallback brain if Gemini is throttled

# --- Speech ---
deepgram-sdk>=3.7.0,<4.0.0    # streaming transcription with speaker separation
edge-tts>=6.1.9               # speech synthesis, including native Filipino and Indonesian voices

# --- Retrieval ---
fastembed>=0.4.0              # sentence embeddings that run locally, no API cost or rate limit
numpy>=1.26                   # vector maths for similarity search
rank-bm25>=0.2.2              # keyword scoring, paired with embeddings for hybrid search
datasketch>=1.6.5             # near-duplicate detection across source documents

# --- Web and document extraction ---
httpx>=0.27                   # HTTP client for scraping and API calls
trafilatura>=1.12             # pulls article text out of a page, dropping nav and footers
selectolax>=0.3.21            # fast HTML parsing where precise selectors are needed
beautifulsoup4>=4.12          # fallback parser for pages the fast path cannot handle
lxml>=5.2                     # parser backend
pypdf>=4.3                    # PDF text extraction
pdfplumber>=0.11              # PDF table extraction, which pypdf does not handle well
python-dateutil>=2.9          # parses the inconsistent date formats found in source content
phonenumbers>=8.13            # detects phone numbers during PII scanning

# --- Service layer ---
fastapi>=0.115                # HTTP and WebSocket endpoints
uvicorn[standard]>=0.30       # server
websockets>=12.0              # client-side streaming to speech services
jinja2>=3.1                   # renders the call interface and the nudge dashboard
python-multipart>=0.0.9       # handles audio file uploads

# --- Audio handling ---
soundfile>=0.12.1             # reads and writes WAV without shelling out
webrtcvad-wheels>=2.0.14      # detects when the caller has stopped speaking
pydub>=0.25.1                 # format conversion and chunking, backed by FFmpeg

# --- Testing ---
pytest>=8.2
